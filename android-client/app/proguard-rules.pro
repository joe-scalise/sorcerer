# The app exposes no JavaScript bridge and relies only on platform/AndroidX APIs.
# Keep source/line metadata so release crash reports remain actionable.
-keepattributes SourceFile,LineNumberTable
-renamesourcefileattribute SourceFile
